// Test stub for the `server-only` package, which throws when imported outside
// a React Server Component. Vitest aliases `server-only` to this no-op so the
// ledger modules can be unit/integration tested in plain node.
export {};
